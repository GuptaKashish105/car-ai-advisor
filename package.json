{
  "name": "car-ai-advisor",
  "private": true,
  "version": "0.0.0",
  "description": "AI-assisted car buying advisor (React + TS client, Express + TS server)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --workspace client\" \"npm run dev --workspace server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
