# Car AI Advisor

A small full-stack app that helps confused car buyers narrow down to a confident shortlist.

- `client/` — React + TypeScript (Vite)
- `server/` — Express + TypeScript

## Development

```bash
npm install
npm run dev
```

Runs both the client and server concurrently.
